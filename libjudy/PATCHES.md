# Patches applied to the bundled libJudy

The sources under `libjudy/` were imported pristine from upstream
Judy-1.0.5 (see [README.md](README.md) for provenance). Every
modification since that import follows one discipline:

- **one patch = one commit**, diffable against the pristine import
  commit (`git log --follow libjudy/` finds it; the import commit
  touches only `libjudy/` and says "pristine" in its subject);
- **one entry in the table below** per patch, stating what changed,
  which files, why, when, and the tracking issue;
- **a per-file change notice** at the top of every modified file
  (a short comment: date + summary of the change), satisfying
  LGPL-2.1 §2(b)'s requirement that modified files carry prominent
  notices of the change and its date.

No reformatting, no drive-by cleanups: a diff against the pristine
import must show only deliberate, documented changes.

The pre-generated table files (`src/Judy1/Judy1Tables.c`,
`src/JudyL/JudyLTables.c`), the build shims under `src/wrappers/`, and
`src/JudyCommon/JudyNoInline.c` (the P7 out-of-line definitions for the
`-DJU_NOINLINE` knob, which upstream declares but never defines) are
php-judy **additions**, not modifications of upstream files — so they
carry their own provenance headers instead of §2(b) change notices.
P7 appears in the ledger below because it repairs upstream behavior
(an unshippable build knob) even though its diff is a new file.

## Patches

| Patch | Category | Files | Why | Date | Issue |
| ----- | -------- | ----- | --- | ---- | ----- |
| P1 | Correctness | `src/JudyCommon/JudyPrivateBranch.h` (plus new pins in the php-judy-addition `src/wrappers/judy_static_asserts.c`) | Judy1 immediate JPs hold up to `cJ1_IMMED1_MAXPOP1` = 15 one-byte indexes written/read contiguously through `jp_1Index` (`JudyCascade.c:604`, `JudyGet.c` IMMED cases), but `j_pi_1Index` was declared with only `sizeof(Word_t)` = 8 bytes — every access past index 7 was undefined behavior, exploited by gcc 15 at `-O3` and gcc 13/14 at `-O2 -funroll-loops` into silent Judy1/BITSET key loss. Widened to `(2 * sizeof(Word_t)) - 1` bytes via a union keeping `j_pi_LIndex` at its old offset (`sizeof(Word_t)`) and `sizeof(jp_t)` unchanged at 16; accessor macros redirected. The immediate-capacity `_Static_assert` pins deferred from Stage 1 (they fail on the pristine layout) now live in `judy_static_asserts.c` and must pass forever after. | 2026-08-18 | [#131](https://github.com/orieg/php-judy/issues/131), [#127](https://github.com/orieg/php-judy/issues/127), [#142](https://github.com/orieg/php-judy/issues/142) |
| P2 | Correctness | `src/JudyCommon/JudyPrivate.h` | The leaf-search method guard read `(!defined(SEARCH_BINARY)) \|\| (!defined(SEARCH_LINEAR))`, so building with `-DSEARCH_LINEAR` still defined `SEARCH_BINARY` and the binary macros (defined later) won — the linear method was unselectable. AND the linear `SEARCHLEAFNONNAT` ignored its `COPYINDEX` parameter, hardcoding `JU_COPY3_PINDEX_TO_LONG`: had the guard been fixed alone, every LEAF5/6/7 search would read only 3 bytes per index — silent data loss (#127: urand16 went 20000→0 hits). Guard flipped to `&&` and `COPYINDEX` honored, landed together deliberately so no intermediate state turns an inert flag into a data-losing one. Default (`SEARCH_BINARY`) builds are textually unaffected. | 2026-08-18 | [#127](https://github.com/orieg/php-judy/issues/127), [#142](https://github.com/orieg/php-judy/issues/142) |
| P3 | Correctness | `src/JudyCommon/JudyInsArray.c` | `JudyLInsArray`/`Judy1SetArray` small-count path allocated `j__udyAllocJLW(Count + 1)`; the allocator's argument is the new Pop1 (cf. `JudyIns.c:1789`'s `pop1 + 1` where `pop1` is the pre-insert population), so `Count + 1` at `Count == cJU_LEAFW_MAXPOP1` (31) indexed one past `j__*_LeafWPopToWords[]` (ASan-confirmed global-buffer-overflow), yielding `Words = 0`, a zero-byte allocation, and two `JU_COPYMEM`s writing 63 words into it — silent heap corruption in a shipped API since 1.0. Secondary: `Count` in {1, 3, 7, 15, 23} over-allocated one `ALLOCSIZES` class (e.g. 47 words instead of 23 at Count 15), disagreeing with `JudyFreeArray.c:86`'s `Pjlw[0] + 1` free size. Fix: allocate `Count`. | 2026-08-18 | [#127](https://github.com/orieg/php-judy/issues/127), [#142](https://github.com/orieg/php-judy/issues/142) |
| P4 | Correctness (UB) | `src/JudyCommon/JudyCascade.c` | `j__udyJLL2toJLB1` (JudyL only) declared `struct _POINTER_VALUES pv[cJU_NUMSUBEXPL]` INSIDE the subexpanse loop body, but the out-of-memory recovery (`while (subexp--)`) reads `pv[]` entries written by earlier iterations — lifetime-expired objects whose values are formally indeterminate, passed to `j__udyLFreeJV()` as pointer + size. Works today only because compilers reuse one stack slot; `-fstack-reuse` policies, ASan `stack-use-after-scope`, or scheduling changes break it. Fix: hoist `pv[]` into a scope enclosing the loop (the minimal-diff form; `j__udyCascade1` recomputes from the bitmap instead, either is correct). OOM-path only — no behavior change on any successful path. | 2026-08-18 | [#127](https://github.com/orieg/php-judy/issues/127), [#142](https://github.com/orieg/php-judy/issues/142) |
| P5 | Portability/Correctness | `src/Judy.h`, `src/JudyCommon/JudyInsArray.c`, `src/JudyCommon/JudyMallocIF.c`, `src/JudyCommon/JudyPrevNextEmpty.c`, `src/JudyCommon/JudyPrivate.h`, `src/JudyCommon/JudyPrivateBranch.h`, `src/JudySL/JudySL.c` | LLP64 (Windows/MSVC x64): `unsigned long` is 4 bytes there, so `Word_t` must be `unsigned __int64` under `_WIN64`, and every all-ones / shifted / masked `UL`/`L` constant used in a `Word_t` context truncated (`~0UL`, `(-1UL)`, `0x100UL`, `0xffL`) or vanished (`1UL << N` and sign-extending `1L << N` for N ≥ 32 — the `JudyPrevNextEmpty` infinite loop). These are the six mechanical fixes CI/release previously applied as download-time regexes, absorbed as real source diffs so the tree builds on Windows unpatched. | 2026-08-18 | [#127](https://github.com/orieg/php-judy/issues/127), [#142](https://github.com/orieg/php-judy/issues/142) |
| P6 | Correctness (latent UB) | `src/JudySL/JudySL.c`, `src/JudyHS/JudyHS.c`, `src/JudyCommon/JudyDel.c` | Hygiene batch, one commit: (1) 32-bit `COPYSTRINGtoWORD` (`JudySL.c`) and `COPYSTRING4toWORD` (`JudyHS.c`) cast each byte to `Word_t` BEFORE shifting — `uint8_t` promotes to `int`, so `byte << 24` overflowed `int` for bytes >= 0x80 (C99 6.5.7p4 UB; the 64-bit siblings already cast first; 32-bit-path-only, never compiled by php-judy's x64 builds). (2) `JudySL.c`'s assert guard was `#ifndef NDEDUG` — a misspelling that unconditionally defined `NDEBUG`, so every assert in the file has been compiled out since 1.0.5; now `#ifndef DEBUG`, the library's own convention (`JudyPrivate.h`: "use -DDEBUG to enable ... assertions"). Note the literal `NDEDUG`→`NDEBUG` spelling fix would have been a semantic no-op (the guard defines `NDEBUG` either way); only the `DEBUG` form makes the asserts reachable. Validated: `-UNDEBUG -DDEBUG` build of the unit runs the full suite + a 50K-key JudySL stress with zero assert failures. (3) `JudyDel.c` top-level BranchL digit scan had no bound under `NDEBUG` (assert-only); now uses the bounded `j__udySearchLeaf1` exactly like the insert path (`JudyIns.c:288`), reporting `JU_ERRNO_CORRUPT` when the digit is absent instead of scanning off `jbl_Expanse[]`. (4) `JudySLIns` advanced `pos2 += WORDSIZE` while `pos2` may be NULL (`NULL + 8` is UB, UBSan pointer-overflow) — now guarded by `Pscl != NULL`, matching the "useless unless Pscl is set" comment. (5) Verify-only: `JudySLPrevSub`'s `~0UL` (`:839`) was already converted to `~(Word_t)0` by P5's tree-wide rule — confirmed, no further change needed. | 2026-08-18 | [#127](https://github.com/orieg/php-judy/issues/127), [#142](https://github.com/orieg/php-judy/issues/142) |
| P7 | Build/Tooling | `src/JudyCommon/JudyNoInline.c` (new php-judy addition; wired into `config.m4`, `config.w32`, `package.xml`) | `JudyPrivate.h` declares `j__udyCountBits{B,L}` and `j__udySearchLeaf{1..7,W}` as `extern` under `-DJU_NOINLINE` (a profiling knob: out-of-line copies are visible to profilers and breakpoints), but upstream ships no definitions anywhere — the knob has never linked. New TU provides the definitions, bodies verbatim from the header's own inline versions, guarded `#ifdef JU_NOINLINE` (compiles to an empty-but-legal TU otherwise, so it is always in the build). Variant-free: these routines are shared between Judy1 and JudyL, hence their unprefixed names. | 2026-08-18 | [#127](https://github.com/orieg/php-judy/issues/127), [#142](https://github.com/orieg/php-judy/issues/142) |
| O1 | Performance | `src/JudyCommon/JudyPrivate.h`, `src/JudyCommon/JudyNoInline.c` | `j__udyCountBits{B,L}` ship hardware-popcount arms only for HP-UX and Linux **Itanium** (Judy predates x86-64 POPCNT, which arrived with SSE4.2 in 2008); every other target falls to the ~15/18-cycle SWAR folds, which sit on the critical path (`JudyGet.c:300`: the popcount result is the offset of the next dependent load, so its latency adds to the descend serially). New arms at the same seam, tried in order: x86-64 gcc/clang under `__POPCNT__` — activated by the `-mpopcnt` configure probe Stage 1 wired into the vendored-only CFLAGS, and deliberately flag-gated because without `-mpopcnt` the builtins emit a libgcc call slower than SWAR; arm64 (`__builtin_popcount{,ll}` lowers to base-ISA `cnt` at any optimization level, no flag needed); MSVC x64 (`__popcnt`/`__popcnt64`, which emit POPCNT unconditionally — hardware POPCNT is **assumed present, i.e. Nehalem 2008 or later**; pre-Nehalem x64 Windows hosts would fault, accepted and documented). The out-of-line `JU_NOINLINE` copies in `JudyNoInline.c` mirror the same arms so a profiling build measures the algorithm production builds run. Builds selecting no arm compile the stock portable folds, textually unchanged. Prior A/B on the extension ([#113](https://github.com/orieg/php-judy/issues/113) round 2, x86-64 Alder Lake): get x0.8290/x0.8414 on popcount-heavy corpora cache-resident, x0.9384 out-of-cache, null on zero-duty-cycle corpora and on the flag-only control; re-measured for this patch in [#142](https://github.com/orieg/php-judy/issues/142). | 2026-08-18 | [#142](https://github.com/orieg/php-judy/issues/142), [#113](https://github.com/orieg/php-judy/issues/113) |
