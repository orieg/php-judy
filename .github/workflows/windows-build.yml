name: Windows Build

on:
  push:
    branches: [ main, fix/windows-build-workflow ]
  pull_request:
    branches: [ main ]
  release:
    types: [published]

jobs:
  build-windows:
    runs-on: windows-latest
    strategy:
      matrix:
        php-version: ['8.1', '8.2', '8.3']
        arch: ['x64']
        ts: ['nts']
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Download PHP Development Package
      run: |
        # Download PHP development package for Windows
        $phpVersion = "${{ matrix.php-version }}"
        $arch = "${{ matrix.arch }}"
        $ts = "${{ matrix.ts }}"
        
        # Use the correct PHP development package URLs
        # For PHP 8.x, we need to use the correct version format
        $phpVersionFormatted = $phpVersion -replace '\.', ''
        
        # Try different URL patterns for PHP development packages
        $devUrls = @(
          "https://windows.php.net/downloads/releases/php-$phpVersion-Win32-vc15-$arch.zip",
          "https://windows.php.net/downloads/releases/php-$phpVersion-Win32-vc16-$arch.zip",
          "https://windows.php.net/downloads/releases/php-$phpVersion-Win32-vc17-$arch.zip"
        )
        
        Write-Host "Trying to download PHP development package for version $phpVersion..."
        
        $phpDir = "C:\php"
        New-Item -ItemType Directory -Path $phpDir -Force
        
        $success = $false
        foreach ($devUrl in $devUrls) {
          Write-Host "Trying URL: $devUrl"
          try {
            $devZip = "php-dev.zip"
            Invoke-WebRequest -Uri $devUrl -OutFile $devZip -TimeoutSec 30
            Expand-Archive -Path $devZip -DestinationPath $phpDir -Force
            $success = $true
            Write-Host "Successfully downloaded and extracted PHP from: $devUrl"
            break
          } catch {
            Write-Host "Failed to download from: $devUrl"
            Write-Host "Error: $($_.Exception.Message)"
          }
        }
        
        if (-not $success) {
          Write-Host "Failed to download PHP development package from all URLs"
          Write-Host "Available files in PHP directory:"
          Get-ChildItem $phpDir | Select-Object Name
          exit 1
        }
        
        # Add PHP to PATH
        $env:Path = "$phpDir;$env:Path"
        
        Write-Host "PHP development package installed to $phpDir"
        Write-Host "Available files:"
        Get-ChildItem $phpDir | Select-Object Name

    - name: Install Visual Studio Build Tools
      run: |
        # Install Visual Studio Build Tools for C++ development
        choco install visualstudio2019buildtools --package-parameters "--add Microsoft.VisualStudio.Workload.VCTools --includeRecommended" -y

    - name: Setup Visual Studio Environment
      run: |
        # Set up Visual Studio environment variables
        $env:Path = "C:\Program Files (x86)\Microsoft Visual Studio\2019\BuildTools\VC\Tools\MSVC\14.29.30133\bin\Hostx64\x64;$env:Path"
        $env:INCLUDE = "C:\Program Files (x86)\Microsoft Visual Studio\2019\BuildTools\VC\Tools\MSVC\14.29.30133\include;$env:INCLUDE"
        $env:LIB = "C:\Program Files (x86)\Microsoft Visual Studio\2019\BuildTools\VC\Tools\MSVC\14.29.30133\lib\x64;$env:LIB"

    - name: Download Judy Library
      run: |
        # Download pre-built Judy library for Windows
        $judyUrl = "https://github.com/orieg/php-judy/releases/download/v2.2.0/judy-windows-lib.zip"
        $judyZip = "judy-lib.zip"
        
        Write-Host "Downloading Judy library..."
        try {
          Invoke-WebRequest -Uri $judyUrl -OutFile $judyZip
          Expand-Archive -Path $judyZip -DestinationPath "C:\judy" -Force
          Write-Host "Judy library installed to C:\judy"
        } catch {
          Write-Host "Judy library not found, will need to build from source"
        }

    - name: Build PHP Extension
      run: |
        # Navigate to the extension directory
        cd ${{ github.workspace }}
        
        # Check if phpize is available
        Write-Host "Checking for phpize..."
        $phpizePath = "C:\php\phpize.exe"
        
        if (Test-Path $phpizePath) {
          Write-Host "Found phpize at: $phpizePath"
          
          # Run phpize
          & $phpizePath
          
          # Configure with Judy
          ./configure --with-judy=shared
          
          # Build the extension
          nmake
          
          # Run tests
          nmake test
          
          Write-Host "Extension build completed"
        } else {
          Write-Host "phpize not found at expected location"
          Write-Host "Available files in PHP directory:"
          Get-ChildItem "C:\php" | Select-Object Name
          exit 1
        }

    - name: Upload build artifacts
      uses: actions/upload-artifact@v4
      with:
        name: judy-windows-${{ matrix.php-version }}-${{ matrix.arch }}-${{ matrix.ts }}
        path: |
          modules/judy.dll
          *.dll
        retention-days: 30

    - name: Create Release Assets
      if: github.event_name == 'release'
      run: |
        # Create release assets for Windows
        $releaseDir = "windows-release"
        New-Item -ItemType Directory -Path $releaseDir -Force
        
        # Copy DLL files
        if (Test-Path "modules/judy.dll") {
          Copy-Item -Path "modules/judy.dll" -Destination "$releaseDir/judy-${{ matrix.php-version }}-${{ matrix.arch }}-${{ matrix.ts }}.dll"
        }
        
        Write-Host "Release assets created in $releaseDir"
